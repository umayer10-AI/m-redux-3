import Counter from '@/component/Counter';
import Display from '@/component/Display';
import React from 'react';

const page = () => {
  return (
    <div>
      <Counter></Counter>
      <Display></Display>
    </div>
  );
};

export default page;